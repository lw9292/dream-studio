# Dream Studio V1.22 ships without fixed API providers or model IDs.
# Configure real connections from Settings → 模型与 API.
PROVIDER_MODE=mock
CORS_ORIGINS=http://localhost:5173
STORAGE_PATH=./data/dream-studio.db
